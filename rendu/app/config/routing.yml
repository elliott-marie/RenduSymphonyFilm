rendu_admin:
    resource: "@renduAdminBundle/Controller/"
    type:     annotation
    prefix:   /

rendu_cinema:
    resource: "@renduCinemaBundle/Controller/"
    type:     annotation
    prefix:   /

app:
    resource: "@AppBundle/Controller/"
    type:     annotation
